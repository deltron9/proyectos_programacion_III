/*
Crea una función llamada crearArray que reciba n números como argumentos 
usando el parámetro rest (...numeros) y devuelva un array.
*Ejemplo: crearArray(1, 2, 3, 4); // [1, 2, 3, 4] *
*/

function crearArray(...numeros){
console.log(numeros);
}
crearArray(1,1,2,3,3,4,5,6,7)