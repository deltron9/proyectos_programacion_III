/*
Realizar una función que reciba un parámetro requerido de tipo numérico y otro opcional de tipo cadena.
Si el segundo parámetro es recibido, se mostrará tantas veces por consola, como lo indique el primer parámetro.
En caso de no recibir el segundo parámetro, se mostrará el valor inverso del primer parámetro. 
*/
function devolverElementos(numero, valor2){
    let = numero
    let = valor2
    if(valor2){
        for(let i = 0; i < numero; i++){
            console.log(valor2)
        }
    }
    else{
        console.log(numero - (numero * 2))
        }
    }
    
    devolverElementos(12, "SEXO")
    console.log("--------------------------------------------------------------------------------------")
    devolverElementos(1)